Структура проекта mybash
1. install.sh — установочный скрипт
Назначение: однократная установка/обновление инструментов.
Что делает:
клонирует или обновляет репозиторий в ~/.mybash-tools;
создаёт ~/.mybashrc на основе шаблона (см. ниже);
безопасно добавляет source ~/.mybashrc в ~/.bashrc (если ещё не добавлено);
(опционально) предлагает установить базовые алиасы для root (/root/.bashrc.local);
не загружает модули при каждом запуске — только при установке.
Зависимости: git, bash.
Не запускается автоматически при открытии терминала.

2. ~/.mybashrc — главный пользовательский конфиг (точка входа)
Назначение: центральное место для настроек и подключения модулей.
Содержимое:
пользовательские переменные (_my_editor, _myos и т.д.);
безопасная загрузка модулей через цикл с проверкой (bash -n);
не содержит логики модулей — только подключение.
Подключается из ~/.bashrc.
Можно редактировать вручную — пользователь управляет, какие модули включены (через комментирование).
3. modules/prompt.sh
Назначение: настройка приглашения командной строки (PS1).
Особенности:
использует переменные вроде _MYBASH_USE_ICONS, _MYBASH_COLOR_MODE;
если переменные не заданы — использует разумные значения по умолчанию;
полностью самодостаточен: можно подключить напрямую в .bashrc.
4. modules/aliases.sh
Назначение: удобные сокращения команд.
Особенности:
использует ${_my_editor:=nano} для безопасного выбора редактора;
содержит алиасы: e, mc, t, ls, update, secret, oops;
можно использовать отдельно.
5. modules/typos.sh
Назначение: исправление частых опечаток (sl, cls, gerp, apt → apt-get и др.).
Особенности:
не зависит от других модулей;
можно отключить, если раздражает;
работает даже без _myos (но использует его, если доступен).
6. modules/functions.sh
Назначение: полезные пользовательские функции.
Содержимое:
info — информация о системе;
extract — универсальная распаковка архивов;
mycmd — список всех доступных команд (будет адаптирован под новый формат!);
alias_check — проверка, какие команды из алиасов отсутствуют в системе.
Самодостаточен, но может использовать _my_editor.
7. modules/completion.sh
Назначение: автодополнение для команд (пока только systemctl).
Особенности:
проверяет наличие системного файла автодополнения;
если нет — задаёт базовый complete -W ...;
безопасен и лёгок.
8. modules/help.sh
Назначение: персональная справочная система.
Особенности:
команда help <тема> → показывает подсказки;
поддерживает темы: ls, vi, key, и любые другие;
help edit → открывает файл справки на редактирование;
данные хранятся в data/help.txt (простой текст, не markdown).
Становится заменой "бумажной шпаргалке".
9. data/help.txt
Назначение: база знаний для help.sh.
Формат:


1
тема команда # пояснение
Пример:


1
2
3
ls ls -a # показать все файлы
vi :q!   # выйти без сохранения
key Ctrl+R # поиск в истории
Легко читается и редактируется вручную.
Не требует парсера — работает через grep.
🔒 Общие принципы всей системы:
Безопасность: ни один модуль не ломает запуск терминала.
Простота: всё на bash, без внешних зависимостей.
Переносимость: достаточно git clone + install.sh.
Гибкость: каждый модуль можно подключить отдельно.
Обучение: пользователь видит, что и как работает — и может править.
