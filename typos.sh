# =============================================================================
# mybash-tools / typos
# Версия: 1.2
# Назначение: Исправление частых опечаток в командах.
# Авторство: Lincooln с активным участием Qwen3-Max
# Зависимости: Нет внешних зависимостей.
# Репозиторий: https://github.com/lincooln/mybash-tools
# Комментарии:
#   - Работает поверх системного command-not-found.
#   - Только для действительно частых опечаток.
# =============================================================================

# @cmd sl — исправляет опечатку: sl → ls
sl() {
    echo -e "\n🚂 Похоже, вы хотели написать \`ls\`?\n"
    command ls "$@"
}

# @cmd cls — исправляет опечатку: cls → clear
cls() {
    echo -e "\n🧹 В Linux команда очистки — \`clear\`.\n"
    command clear
}

# @cmd gerp — исправляет опечатку: gerp → grep
gerp() {
    echo -e "\n🔍 Похоже, вы искали \`grep\`?\n"
    command grep "$@"
}

# @cmd grpe — исправляет опечатку: grpe → grep
grpe() {
    echo -e "\n🔍 Возможно, вы имели в виду \`grep\`?\n"
    command grep "$@"
}

# @cmd mkae — исправляет опечатку: mkae → make
mkae() {
    echo -e "\n⚙️ Команда сборки — \`make\`.\n"
    command make "$@"
}

# @cmd mak — исправляет опечатку: mak → make
mak() {
    echo -e "\n⚙️ Скорее всего, вы хотели \`make\`?\n"
    command make "$@"
}
